import { Section } from '@core/theme';
import { Signature } from '@components';

export const Footer = () => {
  return (
    <footer>
      <Section>
        <Signature />
      </Section>
    </footer>
  );
};
